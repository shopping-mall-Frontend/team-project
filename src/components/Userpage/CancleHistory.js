import React from 'react';
import styled from 'styled-components';

const CancleHistory = () => {
  return <Container>취소내역 컴포넌트</Container>;
};

const Container = styled.div``;

export default CancleHistory;
